# 앱 리뷰 대응 자동화 가이드북

앱스토어·플레이스토어 고객 리뷰를 수집해 AI가 분류하고 답변 초안을 만들고, 사람이 최종 검수해 게시하는 시스템을 만드는 방법. 서비스가 바뀌어도 그대로 쓸 수 있도록 설계했다.

---

## 0. 이 문서 사용법

**대상 독자** — 이 파일을 코딩 에이전트(Claude Code, Cursor 등)에게 넘겨 구현을 시키려는 개발자.

**사용 순서**

1. [14장 워크시트](#14-서비스-적용-워크시트)를 먼저 채운다. 서비스 고유값이 여기 다 들어간다.
2. [2장](#2-사전-준비)의 콘솔 세팅을 직접 한다. 이건 사람이 브라우저로 해야 하는 일이라 에이전트가 대신 못 한다.
3. 이 파일 + 채운 워크시트를 에이전트에게 주고 구현을 요청한다. 요청 문구는 [15장](#15-ai-에이전트에게-요청하는-법)에 있다.

**표기 규칙**

| 표기 | 뜻 |
|---|---|
| 🔒 **고정** | 서비스가 바뀌어도 그대로. 코드에 둔다 |
| 🔧 **가변** | 서비스마다 다름. 설정 파일로 뺀다 |
| ⚠️ | 실제로 사람들이 걸려 넘어지는 지점 |

---

## 1. 무엇을 만드는가

### 1.1 전체 흐름

```
수집 → 정규화 → 분류 → 초안 생성 → 가드레일 → 사람 검수 → 발행 → 축적
                  ↑                                    │
                  └──────── 확정본을 예시로 재투입 ──────┘
```

각 단계는 독립적이다. 한 프롬프트에 "분류하고 태그 달고 답변 써줘"를 넣지 않는다. 나눠야 단계마다 검증이 걸리고, 틀렸을 때 어디서 틀렸는지 안다.

### 1.2 참고 사례

현대차그룹이 2026년 8월 공개한 '고객 리뷰 대응 자동화 솔루션'이 이 구조다. 공개된 내용은 다음과 같다.

| 항목 | 내용 |
|---|---|
| 구조 | 멀티 에이전트 — 분류 → 태깅 → 답변생성이 별개 단계 |
| 안전장치 | Human-in-the-Loop + 3단계 가드레일 |
| 분류 체계 | 17개 언어 · 63개 카테고리 · 119개 태그 |
| 성과 | 건당 35분 → 5분, AI 처리 85%, 분류 정확도 85%, 연 2.6억 절감 |
| 기반 | 사내 LLM 게이트웨이(H Chat Pro) 위의 애플리케이션 |

**공개되지 않은 것** — 3단계 가드레일의 실체, 63개 카테고리의 내용, 모델 선택, 신뢰도 임계값, 검수 화면 형태. 이 가이드북은 그 빈칸을 채운 것이다.

### 1.3 원칙

1. **단계를 쪼갠다.** 검증 지점과 실패 추적을 위해서다.
2. **서비스 고유값은 코드에 넣지 않는다.** 택소노미·톤·금칙어·임계값은 전부 설정이다.
3. **원본은 손대지 않고 통째로 보관한다.** 파생 데이터는 다시 만들 수 있지만 원본은 못 되살린다.
4. **사람이 마지막에 선다.** 자동 발행은 만들지 않는다.

---

## 2. 사전 준비

에이전트가 못 하는 부분이다. 사람이 브라우저로 직접 한다.

### 2.1 Google Play

⚠️ **Play Console의 'API 액세스' 페이지는 없어졌다.** 오래된 문서를 따라가면 헤맨다. 현재 절차는 아래와 같다.

**① GCP — 프로젝트와 API**

1. [Google Cloud Console](https://console.cloud.google.com) 접속
2. 프로젝트 선택 또는 생성
3. **API 및 서비스 → 라이브러리** → `Google Play Android Developer API` 검색 → **사용 설정**

⚠️ 이 단계를 빼먹으면 나중에 403이 난다. 서비스 계정만 만들면 되는 게 아니다.

**② GCP — 서비스 계정과 키**

4. **IAM 및 관리자 → 서비스 계정 → 서비스 계정 만들기**
5. 이름 예: `app-review-automation`
6. **권한(선택사항)과 주 구성원(선택사항)은 비워둔다.** GCP IAM 역할은 필요 없다. 실제 권한은 Play Console에서 준다.
7. 생성된 계정 → **키** 탭 → **키 추가 → 새 키 만들기 → JSON** → 다운로드

⚠️ 이 JSON은 재발급이 안 된다. 잃어버리면 키를 새로 만들어야 한다. gitignore된 위치에 둔다.

**③ Play Console — 권한 부여**

8. Play Console → **사용자 및 권한 → 새 사용자 초대**
9. 이메일에 서비스 계정 주소 입력 (`...@....iam.gserviceaccount.com`)
10. **앱 권한** 탭 → 대상 앱 선택 → **'리뷰에 답글 달기'** 체크
11. **계정 권한** 탭 → **'앱 정보 보기 및 보고서 일괄 다운로드(읽기 전용)'** 체크
12. 저장

⚠️ 두 탭이 다르다. 앱 권한은 리뷰 답변용, 계정 권한은 리포트 버킷용이다. 하나만 주면 다른 쪽이 안 된다.

**④ 리포트 버킷 주소 확인**

13. Play Console → **보고서 다운로드 → 리뷰** → 섹션 제목 옆 **'Cloud Storage URI 복사'**
14. `gs://pubsite_prod_rev_XXXXXXXXXXXXXXXX/reviews/` 형태

⚠️ **버킷 이름의 숫자는 Play Console URL의 개발자 ID와 다르다.** 추측하지 말고 반드시 콘솔에서 복사한다.

⚠️ **버킷 권한은 반영에 최대 24시간 걸린다.** 권한을 준 직후 403이 나면 정상이다. 다음 날 다시 확인한다. `404 = 버킷 이름 오류`, `403 = 권한 미반영`으로 구분해서 판단한다.

**Google Play 제약** 🔒

| 항목 | 값 |
|---|---|
| `reviews.list` 조회 범위 | **최근 7일치만** |
| 과거 데이터 | API 불가. 리포트 버킷 CSV로만 |
| 답변 길이 | **350자** |
| 쿼터 | GET 200/시간, POST 2000/일 |
| 대상 | 운영 버전 + 텍스트 있는 리뷰만 (별점만 남긴 건 API에 안 나옴) |

**필요 스코프**

```
https://www.googleapis.com/auth/androidpublisher      # 리뷰 조회·답변
https://www.googleapis.com/auth/devstorage.read_only  # 리포트 버킷
```

### 2.2 App Store Connect

⚠️ **키 종류를 먼저 고른다. 잘못 고르면 다시 만들어야 한다.**

App Store Connect **사용자 역할**로는 `고객 지원(Customer Support)`이 리뷰 답변 권한을 갖는다. 그런데 **팀 키(Team Key)에는 그 역할을 배정할 수 없다**는 보고가 있어, 팀 키로 답변까지 하려면 `Admin`이 필요하다. 경로가 둘로 갈린다.

**A. 개인 키 — 권장**

`고객 지원` 역할 사용자가 발급한다. 권한이 **평점·리뷰로만 한정**되어 유출 시 피해가 작다.

1. **사용자 및 액세스** → 대상 사용자 선택 (없으면 초대, 역할 `고객 지원`)
2. **추가 리소스** → **‘개별 API 키 생성’** 체크
3. 그 사용자로 로그인 → 키 생성 → `.p8` 다운로드

**B. 팀 키 + Admin — 빠르지만 권한이 넓다**

1. **사용자 및 액세스 → 통합 → App Store Connect API**
2. **팀 키** → `+` → 이름 입력, 액세스 권한 **`Admin`**
3. 생성 → `.p8` 다운로드

⚠️ 리뷰를 읽고 답만 다는 시스템에 Admin 키를 물리는 건 과하다. 조직 승인이 걸려 B로 시작했다면 나중에 A로 교체한다.

**공통으로 챙길 것**

4. **`.p8` 파일** ⚠️ 다운로드는 한 번뿐이다. 잃으면 키를 새로 만들어야 한다
5. **Key ID** — 키 목록에 표시
6. **Issuer ID** — 페이지 상단에 팀 단위로 하나
7. **앱 ID(숫자)** — 앱 페이지 URL의 숫자

**App Store 제약** 🔒

| 항목 | 값 |
|---|---|
| 조회 | `GET /v1/apps/{id}/customerReviews` |
| 답변 | `POST /v1/customerReviewResponses` |
| 응답 | 리뷰당 1개, 교체는 가능 |
| 반영 | 최대 24시간, 그때까지 pending |
| 답변 길이 | Play보다 훨씬 여유롭다. 정확한 값은 구현 시 API 응답으로 확인 |
| 조회 기간 | Play 같은 7일 제한 없음 |
| 공개 RSS(대안) | 인증 불필요, **국가별 500건 상한**, 답변 불가 |

**인증 — ES256 JWT** 🔒

Play와 알고리즘이 다르다. Play는 RS256(RSA), Apple은 **ES256(ECDSA P-256)**이다.

⚠️ **팀 키와 개인 키는 payload 가 다르다.** 팀 키는 `iss`(Issuer ID), 개인 키는 `sub`를 쓴다. 섞으면 401 만 본다.

```
header:  { alg: "ES256", kid: "<Key ID>", typ: "JWT" }

# 팀 키
claims:  {
  iss: "<Issuer ID>",
  iat: <현재 초>,
  exp: <현재 초 + 1200>,      // 최대 20분. 더 길면 거부된다
  aud: "appstoreconnect-v1",
  scope: ["GET /v1/apps"]      // 선택. 넣으면 그 경로만 허용된다
}

# 개인 키 — iss 를 넣지 않는다
claims:  {
  sub: "user",
  iat: <현재 초>,
  exp: <현재 초 + 1200>,
  aud: "appstoreconnect-v1"
}
```

**개인 키의 제약** — 사용자 본인의 역할 권한을 그대로 물려받는다. 그리고 Provisioning, Sales·Finance, notaryTool 엔드포인트는 쓸 수 없다. 리뷰 조회·답변에는 영향이 없다.

⚠️ **개인 키의 권한 = 발급한 사용자의 역할이다.** Admin 계정으로 개인 키를 만들면 결국 Admin 권한 키가 된다. 최소 권한을 원하면 `고객 지원` 역할 사용자를 따로 두고 그 계정으로 발급해야 한다.

`.p8`은 PKCS#8 EC 개인키다. Node에서는 파일 내용을 그대로 `createSign('SHA256')`에 넘기되, **서명을 DER이 아닌 IEEE P1363(r‖s) 형식으로** 뽑아야 한다. Node 15+ 에서는 `dsaEncoding: 'ieee-p1363'` 옵션으로 처리한다.

⚠️ 여기서 대부분 막힌다. DER 서명을 그대로 붙이면 401이 난다.

호출은 `Authorization: Bearer <jwt>`.

**공개 RSS — 믿지 마라**

```
https://itunes.apple.com/{country}/rss/customerreviews/id={appId}/sortby=mostrecent/json
```

인증이 필요 없다는 점만 보고 초기 데이터 확보용으로 쓰고 싶어지지만, **이 피드는 신뢰할 수 없다.**

⚠️ **RSS 가 빈 피드를 HTTP 200 으로 돌려주는데 App Store Connect 에는 리뷰가 멀쩡히 있는 경우가 있다.** 실측 사례 — ASC 기준 텍스트 리뷰 12건인 앱이 kr·us·jp 모든 국가 RSS 에서 0건으로 나왔다. `feed.entry` 키 자체가 없고 에러도 아니다.

그래서 **RSS 결과로 "리뷰가 없다"고 판단하면 안 된다.** 0건이 나오면 반드시 콘솔에서 눈으로 확인한다. 리뷰 규모 파악에는 콘솔 화면이나 정식 API 를 쓴다.

RSS 는 국가별 500건 상한도 있고 답변도 못 단다. 결론적으로 **정식 API 를 기다리는 편이 낫다.**

앱 ID를 모르면 번들 ID로 조회할 수 있다.

```
https://itunes.apple.com/lookup?bundleId={bundleId}&country=kr
```

### 2.3 LLM

경로가 세 가지다. **API 키 발급이 유일한 답은 아니다.**

| 방식 | 무인 실행 | 키 발급 | 적합한 경우 |
|---|---|---|---|
| **코딩 에이전트 헤드리스** (`claude -p` 등) | ✅ | ❌ 불필요 | 구독으로 동작. 클라우드는 장기 토큰 발급 필요 (12.3) |
| **API 키** | ✅ | 필요 | 회사 시스템 편입, 대량 |
| **사내 LLM 게이트웨이** | ✅ | 사내 절차 | 보안 통제가 있는 조직 |

**헤드리스 방식** — 이미 설치된 코딩 에이전트를 스크립트에서 부른다.

```bash
claude -p "<프롬프트>" --output-format json --model sonnet
```

⚠️ **응답이 두 겹으로 싸여 있다. 여기서 대부분 한 번 터진다.**

`--output-format json` 은 **봉투**를 돌려주고, 모델의 실제 답은 그 안 `result` 문자열에 들어 있다. 그리고 그 문자열이 다시 ` ```json ` **펜스로 감싸져 있는 경우가 흔하다.** "JSON만 출력하라"고 지시해도 그렇다.

```
봉투            { "type": "result", "result": "...", "session_id": "..." }
  └ result      "```json\n{ \"category\": ... }\n```"
      └ 펜스 안   { "category": ... }   ← 우리가 원하는 것
```

그래서 파싱이 **두 단계**다.

```ts
// 1단계 — 봉투에서 result 를 꺼낸다. 봉투가 아니면 원문 그대로
let text: string
try {
  text = (JSON.parse(raw) as { result?: string }).result ?? raw
} catch {
  text = raw
}

// 2단계 — 펜스를 벗기고 첫 { 또는 [ 부터 파싱한다
function extractJson<T>(s: string): T {
  const fenced = s.match(/```(?:json)?\s*([\s\S]*?)```/)
  const candidate = (fenced?.[1] ?? s).trim()
  const start = candidate.search(/[[{]/)
  if (start < 0) throw new Error(`JSON 을 찾을 수 없다: ${s.slice(0, 200)}`)
  return JSON.parse(candidate.slice(start)) as T
}
```

⚠️ **초안 생성처럼 자유 텍스트를 받을 때도 펜스를 벗겨야 한다.** 모델이 답변 본문을 코드 블록에 넣어 내놓으면 그대로 스토어에 백틱이 올라간다.

⚠️ **모델이 스키마를 벗어난 값을 낼 수 있다.** 카테고리를 파싱한 뒤 반드시 택소노미에 있는 id 인지 검사하고, 없으면 `fallback` 으로 돌린다. 파서를 믿지 말고 검증한다.

⚠️ **월 리뷰가 수십 건 이하라면 API 키를 따로 발급할 이유가 거의 없다.** 이미 있는 도구로 충분하고, 시크릿을 하나 덜 관리한다.

⚠️ 다만 회사 시스템으로 정식 편입할 때는 API 키나 사내 게이트웨이로 옮긴다. 개인 구독에 회사 운영을 얹으면 안 된다. **LLM 호출부를 인터페이스로 분리해두면 갈아끼우기 쉽다.**

```ts
interface LlmClient {
  complete(prompt: string): Promise<string>
}
// ClaudeCliClient | AnthropicApiClient | InternalGatewayClient
```

**모델 선택** — 분류와 초안에 다른 모델을 써도 된다. 분류는 정형 출력이라 빠른 모델로 충분하고, 초안은 문장 품질이 중요하다.

### 2.4 준비 체크리스트

```
[ ] GCP 프로젝트에 Google Play Android Developer API 사용 설정
[ ] 서비스 계정 생성 + JSON 키 다운로드
[ ] Play Console 앱 권한 — 리뷰에 답글 달기
[ ] Play Console 계정 권한 — 앱 정보 보기 및 보고서 일괄 다운로드
[ ] 리포트 버킷 URI를 콘솔에서 복사
[ ] (24시간 후) 버킷 접근 확인
[ ] ASC 키 종류 결정 (개인 키 권장 / 팀 키는 Admin 필요)
[ ] ASC .p8 다운로드 + Key ID + Issuer ID + 앱 ID
[ ] LLM 접근 경로
[ ] 답변 정책 합의 (3.5 참조)
```

### 2.5 착수 순서 — 무엇이 무엇을 막는가

⚠️ **이 단계에는 사람이 손 놓고 기다려야 하는 구간이 여럿 있다.** 순차로 진행하면 대기 시간이 더해지고, 병렬로 걸어두면 겹친다. 착수 첫날 판단을 잘못하면 며칠이 날아간다.

**대기가 있는 항목**

| 항목 | 대기 | 성격 |
|---|---|---|
| **답변 정책 합의** | 며칠~ | 조직 결정. **가장 오래 걸린다** |
| Play 리포트 버킷 권한 | **최대 24시간** | 구글 측 전파. 손쓸 방법 없음 |
| ASC 사용자 역할 변경 | 조직에 따라 | 개인 키 경로를 택했을 때 |
| App Store 답변 반영 | 최대 24시간 | 발행 단계에서 |

**의존 관계**

```
답변 정책 ─────────────────────────────────→ [발행]
                                              ↑
Play 자격증명 ──→ [API 수집] ──┐               │
              └─→ [버킷] ⏳ ──┼→ [택소노미] → [분류] → [초안] → [검수] ─┘
ASC 자격증명 ──→ [API 수집] ──┘
LLM 키 ───────────────────────────────────→ [분류]
```

**서로 독립인 것 — 아무 순서로나 해도 된다** 🔒

- **Play 설정과 ASC 설정.** 어댑터가 인터페이스로 분리돼 있어 한쪽만 있어도 파이프라인이 돈다. 한쪽을 다 끝내고 다른 쪽으로 넘어갈 이유가 없다.
- **데이터 모델·저장소·CSV 파서 구현.** 자격증명이 하나도 없어도 짤 수 있다.
- **답변 정책 합의.** 코드와 무관하게 첫날 시작한다.

**권장 순서**

```
Day 0  ├─ 답변 정책 논의 착수          ← 가장 오래 걸리므로 제일 먼저
       ├─ Play 자격증명 발급 + 권한 부여 (버킷 권한까지 한 번에)
       ├─ ASC 키 발급 시작
       └─ 저장소·어댑터 뼈대 구현       ← 대기 시간에 진행

Day 1  ├─ 버킷 접근 확인 → 과거 리뷰 벌크 임포트
       └─ 택소노미 설계 (5.3)          ← 실제 리뷰가 필요한 첫 단계

Day 2~ └─ 분류 → 초안 → 가드레일 → 검수
```

⚠️ **Play 권한을 줄 때 앱 권한(리뷰 답글)과 계정 권한(리포트 버킷)을 같이 준다.** 나중에 버킷이 필요해져서 그때 권한을 주면 24시간을 또 기다린다.

⚠️ **키 발급을 기다리는 동안 공개 RSS로 iOS 리뷰를 먼저 받아둘 수 있다.** 택소노미 설계용 데이터를 미리 확보하면 키가 나오는 날 바로 다음 단계로 간다.

---

## 3. 데이터 모델

### 3.1 Review 🔒

수집·정규화의 결과물. 소스가 달라도 이 모양이 된다.

```ts
interface Review {
  id: string                 // 내부 ID
  source: string             // 'google-play' | 'app-store' | ...
  externalId: string         // 스토어의 리뷰 ID
  rating: number             // 항상 1~5로 환산
  title?: string             // iOS만 있는 경우가 많다
  body: string
  language: string           // BCP-47로 통일 (ko, en-US ...)
  country?: string
  appVersion?: string
  device?: string
  authoredAt: Date           // 리뷰 작성 시각 (UTC)
  updatedAt: Date            // 사용자가 수정한 시각 (UTC)
  collectedAt: Date          // 우리가 수집한 시각
  existingReply?: {          // 이미 답변이 달려 있는 경우
    body: string
    repliedAt: Date
  }
  raw: unknown               // 원본 응답 통째로
}
```

⚠️ `raw`를 반드시 남긴다. 분류 기준은 나중에 바뀌고, 그때 스토어에 다시 물어볼 수 없다.

**유니크 키** — `(source, externalId)`. upsert로 처리하고, `updatedAt`이 바뀌면 갱신한다.

### 3.2 Classification 🔒

```ts
interface Classification {
  reviewId: string
  category: string           // 하나만
  tags: string[]             // 여럿 가능
  sentiment: 'positive' | 'neutral' | 'negative'
  urgency: 'low' | 'normal' | 'high'
  confidence: number         // 0~1
  evidence: string           // 판단 근거가 된 원문 구절
  model: string
  classifiedAt: Date
}
```

### 3.3 Draft 🔒

```ts
interface Draft {
  reviewId: string
  body: string
  language: string
  charCount: number
  model: string
  generatedAt: Date
}
```

### 3.4 ReviewCase — 상태 🔒

한 리뷰의 처리 상태를 들고 있는 집계 루트.

```ts
type CaseStatus =
  | 'COLLECTED'      // 수집만 됨
  | 'CLASSIFIED'     // 분류 완료
  | 'DRAFTED'        // 초안 생성됨
  | 'BLOCKED'        // 가드레일에 걸림 → 사람이 직접 작성
  | 'PENDING_REVIEW' // 검수 대기
  | 'APPROVED'       // 승인됨, 발행 대기
  | 'REJECTED'       // 답변 안 하기로 결정
  | 'PUBLISHED'      // 게시 완료
  | 'PUBLISH_FAILED' // 게시 실패, 재시도 대상

interface ReviewCase {
  reviewId: string
  status: CaseStatus
  classification?: Classification
  draft?: Draft
  blocks: GuardrailBlock[]   // 걸린 가드레일 기록
  finalBody?: string         // 사람이 확정한 본문
  editedByHuman: boolean     // 초안에서 수정됐는지
  reviewerNote?: string
  decidedAt?: Date
  publishedAt?: Date
}
```

**상태 전이**

```
COLLECTED → CLASSIFIED → DRAFTED  → PENDING_REVIEW → APPROVED → PUBLISHED
                     ↘                            ↘           ↘
                      BLOCKED → PENDING_REVIEW     REJECTED    PUBLISH_FAILED → (재시도)
```

⚠️ `BLOCKED`는 종착점이 아니다. 초안만 없는 상태로 검수 큐에 들어가고, 사람이 직접 쓴다.

### 3.5 답변 정책 🔧

코드가 아니라 문서로 먼저 합의한다. 이게 없으면 검수자가 승인 버튼을 못 누른다.

```yaml
policy:
  speaker: "브랜드 공식"        # 또는 "담당자 개인"
  formality: "정중한 존댓말"
  signature: "— OO 드림"        # 없으면 빈 문자열
  apology_scope: "불편 사실에 한해 1회"
  can_promise:                 # 답변에서 확정해도 되는 것
    - "고객센터 안내"
  cannot_promise:              # 절대 확정하지 않는 것
    - "환불"
    - "보상"
    - "기능 출시 일정"
  escalation_channel: "고객센터 링크"
  do_not_reply:                # 답변하지 않을 조건
    - "명백한 스팸"
    - "내용 없는 별점만"
```

⚠️ **답변을 한 번도 안 달던 서비스라면, 시스템을 켜는 것 자체가 조직의 의사결정이다.** 개발보다 합의가 오래 걸린다. 가장 먼저 시작한다.

---

## 4. 소스 어댑터

### 4.1 인터페이스 🔒

```ts
interface ReviewSource {
  readonly id: string

  /** 신규·수정된 리뷰를 가져온다 */
  fetchSince(cursor?: string): Promise<{ reviews: RawReview[]; cursor: string }>

  /** 과거 데이터 벌크 임포트. 미지원이면 null */
  bulkImport?(range: { from: Date; to: Date }): Promise<RawReview[]>

  /** 답변 게시. 미지원이면 throw */
  publishReply(externalId: string, body: string): Promise<void>

  /** 플랫폼 제약 — 가드레일이 이걸 읽어 쓴다 */
  readonly constraints: {
    maxReplyLength: number
    retentionDays: number | null   // 조회 가능 기간, 무제한이면 null
    replyEditable: boolean
  }
}
```

새 플랫폼은 이 구현체 하나만 추가한다. 제약을 소스가 스스로 들고 있게 하면 뒤쪽 로직에 플랫폼 분기가 안 생긴다.

### 4.2 Google Play 구현 요점

**인증** — 서비스 계정 JSON으로 RS256 JWT를 만들어 토큰을 받는다. 외부 라이브러리 없이 Node 내장 `crypto`로 충분하다.

```
POST https://oauth2.googleapis.com/token
  grant_type=urn:ietf:params:oauth:grant-type:jwt-bearer
  assertion=<RS256 서명된 JWT>

JWT claims: { iss: client_email, scope, aud: "https://oauth2.googleapis.com/token", iat, exp }
```

**토큰 캐시** — 만료가 1시간이다. 매 호출마다 새로 받지 말고 스코프별로 캐시한다. 스코프가 다르면 토큰도 다르므로 키를 스코프 조합으로 잡는다.

**조회**

```
GET https://androidpublisher.googleapis.com/androidpublisher/v3/applications/{pkg}/reviews
    ?maxResults=100
    &token=<nextPageToken>     # 2페이지부터
```

응답 구조가 **한 겹 더 들어가 있다.** 리뷰 본문이 `reviews[].comments[].userComment` 안에 있다.

```jsonc
{
  "reviews": [{
    "reviewId": "38477664-f68a-41a2-92bb-aefb30884791",
    "comments": [
      {
        "userComment": {
          "text": "재미있어요",
          "lastModified": { "seconds": "1785891441", "nanos": 137000000 },
          "starRating": 5,
          "reviewerLanguage": "ko",
          "device": "gta4xlwifi",
          "appVersionCode": 119,
          "appVersionName": "4.7.0"
        }
      },
      {
        "developerComment": {                    // 이미 답변한 경우에만 있다
          "text": "안녕하세요...",
          "lastModified": { "seconds": "1680570000" }
        }
      }
    ]
  }],
  "tokenPagination": { "nextPageToken": "..." }   // 없으면 마지막 페이지
}
```

⚠️ **`comments` 는 배열이고 순서가 보장되지 않는다.** 인덱스로 꺼내지 말고 `userComment` / `developerComment` 키 존재로 찾는다.

⚠️ **시각이 `{ seconds: "문자열" }` 형태다.** 밀리초가 아니라 초이고 문자열이다. `Number(seconds) * 1000` 으로 변환한다.

⚠️ **페이지네이션 파라미터 이름이 `token` 이다.** `pageToken` 이 아니다. 응답의 `tokenPagination.nextPageToken` 을 받아 다음 요청의 `token` 에 넣는다.

**답변**

```
POST .../applications/{pkg}/reviews/{reviewId}:reply
{ "replyText": "..." }
```

### 4.3 App Store Connect 구현 요점

**인증** — ES256 JWT를 직접 만든다. 토큰 수명이 최대 20분이라 캐시하되 갱신 주기를 짧게 잡는다.

**조회**

```
GET https://api.appstoreconnect.apple.com/v1/apps/{appId}/customerReviews
    ?limit=200
    &sort=-createdDate
    &filter[territory]=KOR        # 선택
```

응답은 JSON:API 형식이다. Play와 모양이 많이 다르다.

```jsonc
{
  "data": [{
    "type": "customerReviews",
    "id": "<리뷰 ID>",
    "attributes": {
      "rating": 5,
      "title": "...",
      "body": "...",
      "reviewerNickname": "...",
      "createdDate": "2026-08-05T00:57:21-07:00",
      "territory": "KOR"
    },
    "relationships": { "response": { ... } }
  }],
  "links": { "next": "..." }
}
```

⚠️ **커서 페이징이다.** `links.next`를 따라간다. Play처럼 페이지 토큰을 직접 만들지 않는다.

⚠️ **기존 답변은 별도 조회다.** `relationships.response`만으로는 본문을 알 수 없다. `include=response`를 붙이거나 `/v1/customerReviews/{id}/response`를 따로 부른다.

⚠️ **`appVersion`이 응답에 없다.** Play에는 있는 필드다. 정규화할 때 `undefined`로 둔다.

**답변**

```
POST https://api.appstoreconnect.apple.com/v1/customerReviewResponses
{
  "data": {
    "type": "customerReviewResponses",
    "attributes": { "responseBody": "..." },
    "relationships": {
      "review": { "data": { "type": "customerReviews", "id": "<리뷰 ID>" } }
    }
  }
}
```

같은 엔드포인트가 생성과 교체를 겸한다. 이미 답변이 있으면 덮어쓴다.

**제약 매핑**

```ts
constraints = {
  maxReplyLength: 5970,   // 구현 시 실제 응답으로 확인할 것
  retentionDays: null,    // Play 같은 7일 제한 없음
  replyEditable: true,
}
```

### 4.4 CSV 벌크 임포트

Play의 7일 제약을 우회하는 유일한 경로다. 리포트 버킷에 월별 CSV가 쌓인다.

```
gs://pubsite_prod_rev_<id>/reviews/reviews_<packageName>_YYYYMM.csv
```

⚠️ **이 CSV는 UTF-16 LE + BOM이다.** UTF-8로 읽으면 통째로 깨진다. BOM을 보고 분기한다.

```ts
function decodeCsv(buf: Buffer): string {
  if (buf[0] === 0xff && buf[1] === 0xfe) return buf.subarray(2).toString('utf16le')  // LE
  if (buf[0] === 0xfe && buf[1] === 0xff) {                                           // BE
    const b = Buffer.from(buf.subarray(2)); b.swap16(); return b.toString('utf16le')
  }
  if (buf[0] === 0xef && buf[1] === 0xbb && buf[2] === 0xbf) return buf.subarray(3).toString('utf8')
  return buf.toString('utf8')
}
```

⚠️ **헤더 첫 컬럼에 BOM 잔재가 남는다.** 떼어내고 비교하지 않으면 `Package Name` 을 못 찾는다.

⚠️ **따옴표 안에 쉼표와 줄바꿈이 들어 있다.** `split(',')` 로는 안 된다. 최소한의 CSV 파서가 필요하다.

**컬럼 매핑**

| CSV 컬럼 | → Review |
|---|---|
| `Review Link`의 `reviewId=` 파라미터 | `externalId` |
| `Star Rating` | `rating` |
| `Review Title` / `Review Text` | `title` / `body` |
| `Reviewer Language` | `language` |
| `App Version Name` / `Device` | `appVersion` / `device` |
| `Review Submit Millis Since Epoch` | `authoredAt` |
| `Review Last Update Millis Since Epoch` | `updatedAt` |
| `Developer Reply Text` + `...Millis` | `existingReply` |

⚠️ `Review Link`에서 뽑은 `reviewId`는 **API가 주는 reviewId와 같은 값**이다. 그래서 CSV 임포트분과 API 수집분을 같은 키로 병합할 수 있다.

---

## 5. 리뷰 유형 카탈로그

### 5.1 표준 유형 14종 🔒

어떤 앱에나 나타나는 유형이다. 여기서 골라 쓰고, 안 나오는 건 버린다.

| # | 카테고리 | 신호가 되는 표현 | 기본 대응 | 자동 초안 |
|---|---|---|---|---|
| 1 | **버그·오류** | 튕겨요, 안 열려요, 오류, 먹통 | 재현 정보 요청 + 채널 안내 | 가능 |
| 2 | **성능·속도** | 느려요, 렉, 배터리, 발열 | 개선 중임 안내 | 가능 |
| 3 | **UI·사용성** | 불편, 못 찾겠, 복잡, 헷갈려 | 의견 수용 + 전달 약속 | 가능 |
| 4 | **기능 요청** | ~있으면, ~해주세요, 추가해 | 검토 약속 (일정 확정 금지) | 가능 |
| 5 | **결제·환불** | 결제, 환불, 취소, 수수료, 이중결제 | **에스컬레이션** | ❌ 차단 |
| 6 | **구독·요금제** | 구독, 자동결제, 해지, 비싸다 | 정책 안내 + 채널 | 조건부 |
| 7 | **계정·로그인** | 로그인, 비밀번호, 계정, 인증 | 채널 안내 | 가능 |
| 8 | **콘텐츠 품질** | 내용, 강의, 상품, 품질 | 도메인별 대응 | 조건부 |
| 9 | **고객지원 불만** | 답이 없, 연락 안, 상담 | **에스컬레이션** | ❌ 차단 |
| 10 | **광고 불만** | 광고, 팝업, 배너 | 정책 안내 | 가능 |
| 11 | **칭찬·만족** | 좋아요, 최고, 감사, 추천 | 짧은 감사 | 가능 |
| 12 | **내용 없음** | 👍, ㅎㅎ, 별점만 | 짧은 감사 또는 미응답 | 가능 |
| 13 | **스팸·무관** | 광고성, 도배, 무관한 내용 | 미응답 | ❌ 미응답 |
| 14 | **기타** | 위 어디에도 안 맞음 | 사람이 판단 | ❌ 차단 |

⚠️ **‘기타’를 반드시 둔다.** 기타 비율이 15%를 넘으면 택소노미가 현실을 못 담고 있다는 신호다.

### 5.2 서비스 유형별 조정 🔧

| 서비스 유형 | 추가할 카테고리 | 특히 조심할 것 |
|---|---|---|
| **커머스** | 배송, 반품, 상품 불일치 | 배송 지연 불만이 압도적으로 많다. 별도 카테고리 필수 |
| **교육·아동** | 수업 품질, 강사, 학습 효과 | 아동 안전 언급은 무조건 차단·에스컬레이션 |
| **게임** | 밸런스, 과금, 서버, 버그 | 과금 불만과 밸런스 불만을 섞지 말 것 |
| **구독 미디어** | 콘텐츠 라인업, 화질, 재생 | 해지 문의가 구독 카테고리에 묻힌다. 분리 |
| **금융** | 인증, 이체, 수수료, 보안 | 금융 사고 언급은 즉시 에스컬레이션. 답변에 금액·절차 언급 금지 |
| **헬스·의료** | 정확도, 기기 연동 | 의학적 조언으로 읽힐 표현 금지 |

### 5.3 택소노미 설계 절차 🔧

**리뷰를 읽기 전에 카테고리를 정하면 반드시 현실과 어긋난다.**

1. 실제 리뷰를 최소 100건 읽는다. 100건이 안 되면 있는 것 전부.
2. 비슷한 것끼리 손으로 묶는다. 이름은 나중에 붙인다.
3. 묶음이 **5~8개**가 되도록 합치거나 쪼갠다.
4. 각 대분류 아래 태그를 3~6개 둔다.
5. 5.1 표준 목록과 대조해 빠진 게 없는지 본다.
6. '기타'를 추가한다.

**설정 형식**

```yaml
taxonomy:
  categories:
    - id: bug
      label: "버그·오류"
      description: "앱이 의도대로 동작하지 않음"
      tags: [crash, freeze, blank_screen, sync_fail]
      auto_draft: true
    - id: payment
      label: "결제·환불"
      description: "결제 실패, 환불 요구, 요금 이의"
      tags: [refund_request, double_charge, fee_complaint]
      auto_draft: false        # 가드레일 ①에서 차단
  fallback: other
```

⚠️ **카테고리가 많을수록 정확도가 떨어진다.** 현대차는 63개지만 월 수천 건 규모다. 못 쓰는 63개보다 쓰는 6개가 낫다.

⚠️ **상호배타적으로 이름 짓는다.** "앱 오류"와 "결제 실패"가 겹치면 모델이 매번 다르게 찍는다.

---

## 6. 분류 에이전트

### 6.1 프롬프트 템플릿 🔒

구조는 고정, `{{ }}` 안은 워크시트에서 채운다.

````
당신은 {{SERVICE_NAME}} 앱의 고객 리뷰를 분류하는 분석가다.
리뷰를 읽고 아래 스키마에 맞는 JSON만 출력한다. 다른 말은 쓰지 않는다.

## 카테고리 (하나만 고른다)
{{CATEGORY_LIST}}

## 태그 (해당하는 것 모두)
{{TAG_LIST}}

## 서비스 맥락
{{SERVICE_CONTEXT}}

## 판단 규칙
- category 는 반드시 위 목록에서 고른다. 애매하면 "other".
- evidence 에는 판단 근거가 된 원문 구절을 그대로 인용한다. 요약하지 않는다.
- confidence 는 스스로 얼마나 확신하는지다. 애매하면 낮게 준다.
  0.9 이상 = 명확함 / 0.7~0.9 = 대체로 확실 / 0.7 미만 = 애매함
- urgency:
    high   = 결제 사고, 안전, 법적 분쟁, 서비스 전면 장애
    normal = 기능 오류, 사용 불편
    low    = 칭찬, 단순 의견
- 리뷰가 여러 주제를 담으면 가장 강한 불만을 category 로 잡고 나머지는 tags 로 둔다.

## 출력 스키마
{
  "category": "<id>",
  "tags": ["<id>", ...],
  "sentiment": "positive" | "neutral" | "negative",
  "urgency": "low" | "normal" | "high",
  "confidence": 0.0~1.0,
  "evidence": "<원문 인용>"
}

## 입력
별점: {{RATING}}/5
앱 버전: {{APP_VERSION}}
언어: {{LANGUAGE}}
리뷰: """
{{REVIEW_BODY}}
"""
````

### 6.2 근거 인용을 요구하는 이유

원문에서 근거를 찾아 적게 하면 환각이 줄고, **검수자가 왜 그렇게 분류했는지 한눈에 본다.** 검수 속도에 직접 영향을 주는 장치라 빼면 안 된다.

### 6.3 신뢰도 다루기 🔧

```yaml
confidence:
  auto_threshold: 0.75    # 미만이면 자동 흐름에서 빼고 검수 대기
```

운영하면서 조정한다. 처음엔 높게 잡고(0.85) 검수하며 내린다.

---

## 7. 초안 에이전트

### 7.1 프롬프트 템플릿 🔒

````
당신은 {{SERVICE_NAME}}의 공식 답변 담당자다.
아래 고객 리뷰에 대한 답변을 쓴다. 답변 본문만 출력한다.

## 화자와 톤
- 화자: {{SPEAKER}}
- 말투: {{FORMALITY}}
- 맺음: {{SIGNATURE}}

## 반드시 지킬 것
- **{{REVIEW_LANGUAGE}} 로 쓴다.** 리뷰와 같은 언어다.
- **{{MAX_LENGTH}}자를 넘지 않는다.** 공백 포함이다.
- 확인할 수 없는 사실을 단정하지 않는다.
- 다음은 절대 확정하지 않는다: {{CANNOT_PROMISE}}
- 사과는 불편한 사실에 대해 한 번만. 반복하지 않는다.
- 고객이 쓴 표현을 한 번은 받아 적어 읽었다는 신호를 준다.

## 하지 말 것
- "죄송합니다"를 두 번 이상 쓰기
- 구체적 일정이나 버전을 약속하기
- 템플릿처럼 읽히는 상투구로만 채우기
- 링크나 연락처를 임의로 만들어 쓰기 (허용: {{ALLOWED_LINKS}})

## 분류 결과 (참고)
카테고리: {{CATEGORY}}
태그: {{TAGS}}
감정: {{SENTIMENT}}
근거: {{EVIDENCE}}

## 카테고리별 지침
{{CATEGORY_GUIDANCE}}

## 좋은 답변 예시
{{FEW_SHOT_EXAMPLES}}

## 입력
별점: {{RATING}}/5
리뷰: """
{{REVIEW_BODY}}
"""
````

### 7.2 카테고리별 지침 🔧

카테고리마다 답변의 방향이 다르다. 설정으로 둔다.

```yaml
category_guidance:
  bug: "재현 상황(기기·버전·상황)을 물어보고 문의 채널을 안내한다. 원인을 추측해 단정하지 않는다."
  feature_request: "의견을 담당 팀에 전달하겠다고만 한다. 도입 여부나 일정은 언급하지 않는다."
  praise: "짧게 감사한다. 3문장을 넘기지 않는다. 홍보 문구를 덧붙이지 않는다."
  empty: "한 문장으로 감사한다."
```

### 7.3 예시 풀 관리 🔧

**시작 전에 기존 답변부터 찾는다.** 🔒

과거에 사람이 직접 단 답변이 남아 있다면 그게 최고의 few-shot 재료다. 실제로 그 브랜드가 쓴 말투이고, 이미 검수를 통과한 문장이다. 새로 톤을 정의하는 것보다 있는 걸 뽑아 쓰는 편이 훨씬 정확하다.

⚠️ **한쪽 스토어에 답변이 없다고 다른 쪽도 없다고 단정하지 마라.** 담당자가 한 플랫폼만 관리했을 수 있다. 실측 사례 — Play 는 37건 전부 미답변인데 App Store 에는 답변이 여러 건 달려 있었다.

기존 답변에서 뽑아낼 것

- 인사말과 맺음말 고정 문구
- 자기 지칭 (“저희 OO팀”, 브랜드명)
- 사과의 강도와 빈도
- 이모지 사용 여부
- 무엇을 약속하고 무엇을 피했는지

이걸 3.5 답변 정책에 그대로 반영하면 정책 합의가 크게 짧아진다. **“새로 정하자”가 아니라 “하던 대로 하자”가 되기 때문이다.**

기존 답변이 없으면 `{{FEW_SHOT_EXAMPLES}}`를 비워두고 시작한다. 검수에서 **수정 없이 승인된 답변**이 쌓이면 카테고리별로 2~5건씩 채운다. 이게 8장의 피드백 루프다.

### 7.4 길이는 상한이 아니라 목표로 준다 🔒

플랫폼마다 답변 길이 제한이 다르다. App Store 는 넉넉하고 Google Play 는 **350자**다. 여기서 두 가지 문제가 생기는데, **둘째가 훨씬 자주 걸린다.**

**문제 ① 긴 예시를 주면 길게 쓴다**

여유로운 플랫폼에서 쓰던 답변을 예시로 주면 모델이 그 길이를 따라 쓴다. 제한이 빡빡한 쪽에서는 잘린다.

→ **예시 풀을 플랫폼별로 거른다.** 대상 제한을 넘는 예시는 넣지 않는다.

**문제 ② 제한을 상한으로 주면 그만큼 늘어난다** ⚠️

실측 — 기존 답변이 192~403자(대부분 300자 내외)인 서비스에서, `maxLength` 를 플랫폼 최대치(5970자)로 주고 초안을 뽑으니 **492자가 나왔다. 기존 어떤 답변보다도 길다.** 제한에 안 걸렸을 뿐 톤이 무너진 것이다. 같은 리뷰를 350자 제한으로 뽑으니 266자로, 기존 답변들과 비슷한 호흡이 나왔다.

→ **`maxLength` 는 "잘리지 않는 한계"가 아니라 "이 정도로 써라"는 목표다.** 플랫폼 제한을 그대로 넣지 말고, 기존 답변의 실제 길이 분포를 보고 정한다.

```ts
// ❌ 플랫폼 제한을 그대로
maxLength: source.constraints.maxReplyLength

// ✅ 원하는 길이. 플랫폼 제한은 가드레일 ②가 따로 검사한다
maxLength: Math.min(targetLength, source.constraints.maxReplyLength)
```

`targetLength` 는 기존 답변 길이의 중앙값 근처로 잡으면 무난하다.

**공통 대응**

- 짧은 예시가 부족하면 **긴 예시를 줄인 짧은 버전을 만들어 짝지어 넣는다.** 톤은 그대로 두고 길이만 줄인 쌍을 보여주면 "같은 목소리로 짧게"를 배운다.
- **요약을 시키지 말고 처음부터 짧게 쓰게 한다.** 긴 답변을 만든 뒤 줄이면 인사와 맺음이 잘려나가 톤이 무너진다.
- 길이 검증은 프롬프트가 아니라 **가드레일 ②에서 기계적으로** 한다.

⚠️ **길이를 프롬프트로 부탁하면 안 지킨다.** 반드시 가드레일에서 기계적으로 검증한다.

---

## 8. 가드레일

한 곳에서 다 막으려 하면 어디서 걸렸는지 모른다. 성격이 다른 세 지점에 나눈다.

**모든 차단의 결과는 "거부"가 아니라 "사람에게 넘김"이다.** 🔒

### 8.1 ① 입력 게이트 — 초안 생성 자체를 막는다

답변을 잘못 쓰면 위험한 리뷰는 AI가 손대지 않는다.

```yaml
input_gate:
  block_categories: [payment, support_complaint, other]
  block_keywords:
    - 소송
    - 고소
    - 변호사
    - 언론
    - 국민신문고
    - 개인정보
  block_if:
    - rating: 1
      and_sentiment: negative
      and_urgency: high
  # 아동 서비스라면 추가
  # - 안전, 다쳤, 부상
```

### 8.2 ② 출력 검증 — 기계적으로 확인한다

```yaml
output_check:
  max_length: from_source          # 소스 constraints 에서 읽는다
  must_match_review_language: true
  forbidden_phrases:
    - "환불해 드리겠습니다"
    - "보상해 드리겠습니다"
    - "곧 출시됩니다"
    - "다음 업데이트에서"
  forbidden_patterns:
    - "https?://(?!{{ALLOWED_DOMAINS}})"   # 허용 외 링크
    - "\\d{2,3}-\\d{3,4}-\\d{4}"           # 전화번호
  max_apology_count: 1
```

### 8.3 ③ 신뢰도 임계 — 애매하면 멈춘다

```yaml
confidence_gate:
  min_confidence: 0.75
  action: hold_for_review
```

### 8.4 차단 기록 🔒

```ts
interface GuardrailBlock {
  stage: 'input' | 'output' | 'confidence'
  rule: string        // 어떤 규칙에 걸렸는지
  detail: string      // 걸린 값
  blockedAt: Date
}
```

⚠️ **차단 사유를 남기지 않으면 가드레일은 튜닝 불가능한 블랙박스가 된다.** 운영 초기엔 차단 사례를 모아 보는 것 자체가 산출물이다.

---

## 9. 검수 화면

사람이 마지막에 서는 지점이다. **여기 UX가 나쁘면 시스템 전체가 안 쓰인다.**

### 9.1 원칙

1. **한 화면에서 판단이 끝나야 한다.** 다른 탭을 열게 만들면 검수가 밀린다.
2. **읽는 순서 = 판단 순서.** 원문 → 분류 → 초안 순으로 위에서 아래로 배치한다.
3. **기본 동작은 승인이다.** 초안이 대체로 쓸 만해야 하고, 승인이 가장 적은 동작이어야 한다.
4. **되돌릴 수 있어야 한다.** 발행은 비가역이므로 발행 전 마지막 확인을 둔다.

### 9.2 화면 구성

```
┌───────────────────────────────────────────────────────────────┐
│  검수 대기 12건          [전체] [높은 긴급도] [차단됨] [낮은 별점]│
├───────────────────────────────────────────────────────────────┤
│                                                               │
│  ★☆☆☆☆   2026-08-05  ·  Android 4.7.0  ·  Galaxy S25   [ko]  │
│                                                               │
│  ┌─ 리뷰 원문 ────────────────────────────────────────────┐  │
│  │ 결제한 금액이 환불도 안 되고 쓸 곳도 없고...             │  │
│  │ 어찌 해야되나요 너무 별로예요 ㅠㅠ 거기다 카드랑         │  │
│  │ 했을때랑 결제 금액이 다르잖아요                          │  │
│  │         ~~~~~~~~~~~~~~~~~~~~  ← 근거 구절 하이라이트     │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                               │
│  분류   [결제·환불]  #refund_request #fee_complaint            │
│         부정 · 긴급도 높음 · 신뢰도 0.92                       │
│                                                               │
│  ⛔ 가드레일 차단 — 입력 게이트                                │
│     결제·환불 카테고리는 자동 초안을 만들지 않습니다.          │
│     직접 작성하거나 고객센터로 이관하세요.                     │
│                                                               │
│  ┌─ 답변 ─────────────────────────────────────── 0 / 350 ─┐  │
│  │ (직접 작성)                                             │  │
│  │                                                         │  │
│  └─────────────────────────────────────────────────────────┘  │
│                                                               │
│  [승인 ⏎]  [반려 R]  [보류 H]  [고객센터 이관 E]              │
│                                            ← 이전 J   다음 K → │
└───────────────────────────────────────────────────────────────┘
```

### 9.3 요소별 명세

| 요소 | 요구사항 |
|---|---|
| **별점** | 색으로도 구분. 1~2점은 붉게 |
| **원문** | 전문 표시. 접지 않는다. **근거 구절을 하이라이트** |
| **분류** | 카테고리는 칩, 태그는 작게. 신뢰도가 임계 미만이면 경고색 |
| **차단 배너** | 걸린 단과 사유를 사람 말로. "규칙 R-03 위반" 같은 코드만 쓰지 않는다 |
| **답변 편집기** | 항상 편집 가능. 초안이 채워진 상태로 시작 |
| **글자 수** | 실시간. 제한의 90% 넘으면 주황, 초과하면 빨강 + 승인 비활성 |
| **액션** | 승인 / 반려 / 보류 / 이관. 반려엔 사유 선택지 |

### 9.4 정렬과 필터 🔧

기본 정렬은 **손이 필요한 순서**다. 최신순이 아니다.

```
1. 가드레일 차단 (사람이 직접 써야 함)
2. 긴급도 high
3. 별점 1~2점
4. 신뢰도 임계 미만
5. 나머지 — 오래된 순
```

### 9.5 키보드

리뷰가 많으면 마우스로는 못 버틴다.

| 키 | 동작 |
|---|---|
| `J` / `K` | 이전 / 다음 |
| `⏎` | 승인 |
| `E` | 편집 포커스 |
| `R` | 반려 |
| `H` | 보류 |
| `⌘⏎` | 편집 중 저장 후 승인 |

### 9.6 발행 결정

승인이 곧 발행이 아니다. 한 단계 더 둔다.

```
검수 승인  →  [발행 대기 목록]  →  일괄 확인  →  발행
```

이유는 두 가지다. **발행은 되돌리기 어렵고**, 여러 건을 한 번에 훑어보면 톤이 들쭉날쭉한 걸 발견하기 쉽다.

발행 대기 화면에는 이것만 있으면 된다 — 리뷰 한 줄 요약, 확정된 답변, 대상 스토어, [발행] / [검수로 되돌리기].

### 9.7 슬랙을 검수 화면으로 쓰기

화면을 따로 만들지 않고 슬랙 채널을 검수 창구로 쓸 수 있다. **검수는 보러 가는 것보다 찾아오는 편이 실제로 잘 돌아간다.** 리뷰가 드문드문 들어오는 서비스라면 전용 화면을 만들어도 아무도 안 들어간다.

**구조**

```
채널 메시지   ★☆☆☆☆  "결제한 금액 환불이 안 돼요…"
              Google Play · 2026-08-05 · v4.7.0
  └ 스레드     결제·환불  `환불 요구`
              부정 · 긴급도 높음 · 신뢰도 0.92
              > 환불도 안되고 남은 금액 쓸곳도 없고

              ⛔ 확인 필요
              • 결제·환불 카테고리는 자동 초안을 만들지 않습니다.

              [ 답변 등록하러 가기 ]   ← 콘솔 리뷰 상세로 직행
```

리뷰는 채널에, 검수 내용은 스레드에 둔다. 채널이 리뷰 목록처럼 남고 판단 재료는 접혀 있어 쌓여도 읽을 만하다.

**⚠️ Incoming Webhook 으로는 스레드를 못 단다.** 웹훅은 메시지 `ts` 를 돌려주지 않는다. 봇 토큰(`chat:write`)으로 `chat.postMessage` 를 호출해 `ts` 를 받고, 그걸 `thread_ts` 로 넘겨야 코멘트가 붙는다.

**스레드는 두 메시지로 나눈다** 🔒

```
스레드 ①   분류 · 태그 · 신뢰도 · 근거 · 가드레일 경고
스레드 ②   초안 (코드블록) + [답변 등록하러 가기]
```

초안을 분류 결과와 같은 메시지에 넣으면 어디부터 어디까지가 답변인지 눈으로 잘라내야 한다. 메시지를 나누면 **초안만 따로 다룰 수 있고 버튼도 초안 바로 아래 붙는다.**

⚠️ **초안은 코드블록으로 감싼다.** 일반 텍스트로 두면 주변 설명과 섞여 어디까지 복사해야 할지 애매해진다. 코드블록은 경계가 뚜렷하다.

⚠️ **다만 슬랙 코드블록에 복사 버튼이 항상 뜨지는 않는다.** 클라이언트와 버전에 따라 다르다. 그래도 시각적 구분이 주는 이득이 더 크다는 것이 실제 사용자 판단이었다. 한글은 모노스페이스에서 자간이 벌어지는데, 그 단점을 감수할 만하다.

**답변 등록 링크** — 버튼을 눌러 콘솔에서 직접 등록하게 하면 발행 API 를 안 붙여도 된다.

- Google Play: 리포트 CSV 의 `Review Link` 컬럼이 리뷰별 상세 URL 이다
- App Store Connect: 리뷰별 앵커가 없어 앱 리뷰 목록으로 보낸다. 스레드에 리뷰 제목이 있으면 찾을 수 있다

**편집 이력은 스토어에서 되읽어 복원한다** 🔒

버튼이 없으니 "사람이 초안을 얼마나 고쳤는지"를 직접 못 잡는다. 9.3 에서 개선의 유일한 재료라고 한 그것이다.

**다음 수집 주기에 스토어가 알려준다.** 발행된 답변이 `existingReply` 로 들어오므로 우리 초안과 비교하면 된다.

```
초안    "안녕하세요 고객님, OO 입니다. …"
실제    (다음 ingest 의 existingReply)
        → diff → 초안 채택률
```

오히려 이 편이 정확하다. 버튼 클릭이 아니라 **실제로 발행된 문장**을 재기 때문이다.

**대사(reconcile) 는 수집 직후에 돌린다** 🔒

이걸 빼먹으면 세 가지가 동시에 망가진다. 실제로 겪은 순서대로 적는다.

1. **케이스가 검수 큐에서 영원히 빠지지 않는다.** 사람이 콘솔에서 답했는데 우리 상태는 여전히 "검수 대기" 다. 큐 건수가 계속 늘어 몇 달 뒤엔 의미 없는 숫자가 된다.
2. **초안 채택률을 측정할 수 없다.** 유일한 개선 지표가 영원히 비어 있다.
3. **미답변 방치를 알아챌 수 없다.** 진짜 안 한 것과 이미 한 것이 섞여 있으니 어느 쪽도 모른다.

```
수집 → 대사 → (분류 → 초안 → 알림)
       ↑
       스토어에 답변이 새로 달린 케이스를 찾아
       PUBLISHED 로 바꾸고 초안 vs 실제를 기록한다
```

⚠️ **멱등해야 한다.** 매일 도는 작업이므로 같은 리뷰를 두 번 집계하면 채택률이 오염된다. 이력에 이미 있으면 건너뛴다.

⚠️ **초안이 없던 건은 채택률 분모에서 뺀다.** 가드레일에 막혀 사람이 직접 쓴 것은 "초안을 안 썼다" 가 아니라 "초안이 없었다" 이다. 섞으면 채택률이 실제보다 낮게 나온다.

**한계** — 되읽기가 한 주기 늦으므로 지표가 실시간이 아니다. 그리고 사람이 콘솔에서 초안을 안 보고 새로 써버리면 참고 여부를 알 수 없다. 채택률이 유난히 낮게 나오면 이 가능성을 의심한다.

**미답변 리마인더** — 알림은 한 번만 가므로 슬랙에서 밀려 내려가면 그걸로 끝이다. 대사가 있으면 주간 heartbeat 의 "미답변 N건" 이 정확해져서 그게 리마인더 역할을 한다. 매일 재알림하면 시끄럽고, 주 1회 요약이 적당하다.

### 9.8 화면에 뿌리기 전에 사람 말로 바꾼다 🔒

분류 결과는 모델이 다루기 좋은 형태로 저장되지만, **검수 화면은 사람이 읽는다.** 그대로 뿌리면 검수 속도가 떨어진다.

| 저장 | 화면 |
|---|---|
| `praise` `general_praise` | 칭찬·만족 · 전반 만족 |
| `positive` · `low` | 긍정 · 긴급도 낮음 |

⚠️ **id 는 영문으로 두고 표시할 때만 라벨로 바꾼다.** 프롬프트에 한글 id 를 쓰면 모델 출력이 흔들린다. 택소노미에 `label`, 태그에 `tag_labels` 맵을 따로 둔다.

⚠️ **기기 정보는 코드명으로 온다.** Google Play API 는 마케팅명이 아니라 내부 코드명을 준다(`gta4xlwifi` = Galaxy Tab S6 Lite, `pa3q` = Galaxy S25 Ultra). 콘솔 화면은 변환해서 보여주지만 API 는 원본이다.

사람이 읽을 수 없으므로 **원인 파악이 필요한 카테고리(버그·사용성)에서만 노출한다.** 칭찬 리뷰에 기기명은 노이즈다. 정확한 이름이 꼭 필요하면 Google 이 배포하는 기기 카탈로그로 매핑해야 하는데, 파일이 크고 관리 부담이 있으니 실제로 필요해질 때 붙인다.

**날짜는 `YYYY-MM-DD` 로 통일한다.** 로케일 기본 포맷(`2026. 8. 5.`)은 자릿수가 들쭉날쭉해 목록에서 정렬돼 보이지 않는다.

### 9.9 안티패턴 ⚠️

- **승인 버튼만 있고 편집 이력을 안 남긴다** → 8장 피드백 루프를 통째로 못 한다. 가장 흔하고 가장 치명적이다.
- **차단 사유를 코드로만 표시** → 검수자가 무슨 뜻인지 몰라 보류만 누른다.
- **반려 사유가 자유 입력뿐** → 집계가 안 돼서 개선점을 못 찾는다.
- **판단 기준이 화면에 없다** → 답변 정책을 사이드바나 링크로 항상 보이게 둔다.
- **한 번에 여러 건을 자동 승인** → 검수의 의미가 사라진다. 일괄 승인은 만들지 않는다.

---

## 10. 발행

### 10.1 규칙 🔒

```ts
async function publish(case: ReviewCase, source: ReviewSource) {
  if (case.status !== 'APPROVED') throw new Error('승인되지 않음')
  if (case.publishedAt) return                       // 멱등
  if (case.finalBody.length > source.constraints.maxReplyLength) throw ...

  try {
    await source.publishReply(case.reviewId, case.finalBody)
    case.status = 'PUBLISHED'
    case.publishedAt = new Date()
  } catch (e) {
    case.status = 'PUBLISH_FAILED'
    // 사유 기록, 재시도 큐로
  }
}
```

⚠️ **반영이 즉시가 아닐 수 있다.** App Store는 최대 24시간 pending이다. 게시 직후 조회해서 없다고 다시 쏘면 중복된다. 반드시 로컬 상태를 먼저 믿는다.

### 10.2 확인

발행 후 다음 수집 주기에 원본을 다시 읽어 `existingReply`가 채워졌는지 확인한다. 안 채워졌으면 알림을 띄운다.

---

## 11. 측정과 개선

### 11.1 지표 🔒

| 지표 | 정의 | 언제 쓰나 |
|---|---|---|
| **초안 채택률** | 수정 없이 승인된 비율 | 항상. 표본이 적어도 의미 있다 |
| **편집 거리** | 초안 vs 최종본 차이 | 어느 카테고리가 특히 안 맞는지 |
| **분류 정확도** | 정답 라벨 대비 일치율 | 정답 세트 100건 이상일 때만 |
| **기타 비율** | `other`로 분류된 비율 | 15% 넘으면 택소노미 갱신 |
| **차단율** | 가드레일에 걸린 비율 | 규칙이 과한지 느슨한지 |
| **처리 시간** | 수집 → 발행까지 | SLA가 있다면 |

### 11.2 볼륨이 작은 서비스 ⚠️

**월 리뷰가 수십 건 이하라면 분류 정확도는 포기하고 초안 채택률로 간다.**

정확도는 정답 세트가 100건 이상 있어야 통계적으로 의미가 생긴다. 표본이 20건인데 정확도 85%라고 말하면, 3건만 달랐어도 70%가 된다. 숫자가 우연히 흔들려서 오히려 잘못된 판단을 하게 된다.

채택률은 첫 10건부터 읽을 수 있고 해석도 단순하다 — 낮으면 초안이 나쁜 것이다.

### 11.3 피드백 루프 🔒

| 관찰 | 조치 |
|---|---|
| 수정 없이 승인된 답변 | few-shot 예시 풀에 추가 |
| 특정 카테고리가 많이 고쳐짐 | 해당 `category_guidance` 보강 |
| `other` 누적 | 새 카테고리 신설 검토 |
| 차단이 과함 | 규칙 완화 |
| 차단을 뚫고 나쁜 초안이 나옴 | 규칙 추가 |

---

## 12. 자동 실행

파이프라인을 다 만들어도 **누군가 주기적으로 돌려야** 한다. 이 장이 빠지면 "손으로 명령을 치는 도구"에서 끝난다.

### 12.1 무엇을 돌리는가

```
ingest  →  process  →  notify
수집·저장   분류·초안·가드레일   알림
```

세 단계를 순서대로 돌린다. **수집이 실패하면 뒤는 의미가 없으므로 거기서 멈춘다.** 처리가 실패해도 마찬가지다. 알림은 실패해도 데이터는 남으니 다음 실행에서 다시 시도된다.

새 리뷰가 없으면 아무 일도 일어나지 않아야 한다. 매일 돌려도 조용한 게 정상이다.

### 12.2 실시간은 안 된다 — 폴링뿐이다 🔒

"리뷰가 올라오면 바로 알림" 을 원하게 되지만, **스토어가 리뷰 푸시를 지원하지 않는다.**

| | 푸시 | 대상 |
|---|---|---|
| Google Play RTDN (Pub/Sub) | 있음 | **구독·결제 상태만.** 리뷰 없음 |
| App Store Connect 웹훅 | 있음 | 앱 심사 상태·빌드·TestFlight 피드백. **고객 리뷰 없음** |

둘 다 웹훅 기능 자체는 있어서 헷갈리기 쉽다. **이벤트 목록에 고객 리뷰가 없다.**

그래서 폴링 주기를 줄이는 게 유일한 방법이다.

| 주기 | 지연 | Google Play 쿼터 (GET 200/시간) |
|---|---|---|
| 1일 | 최대 24시간 | 1회 |
| 1시간 | 최대 1시간 | 24회 |
| 15분 | 최대 15분 | 96회 |
| 5분 | 최대 5분 | **288회 — 초과** |

**LLM 호출은 새 리뷰가 있을 때만 일어나므로 주기를 줄여도 비용이 늘지 않는다.** 대부분의 실행은 "새 리뷰 없음" 으로 끝난다.

⚠️ **GitHub Actions 는 짧은 주기에 부적합하다.** 최소 5분이라고 되어 있어도 실제로는 부하에 따라 수십 분 밀린다. 지연이 중요하면 Cloud Run Job + Cloud Scheduler 처럼 정확한 스케줄러를 쓴다.

**주기는 볼륨으로 정한다.** 월 몇 건 수준이면 하루 1회로 충분하다. 리뷰가 드문데 15분마다 도는 건 대부분 헛돈다.

### 12.3 스케줄러 선택은 LLM 인증 방식에 묶인다 ⚠️

**이게 가장 자주 걸리는 지점이다.** 스케줄러를 먼저 고르고 나중에 LLM 을 붙이려다 막힌다.

| LLM 호출 방식 | 로컬 스케줄러 | 클라우드 |
|---|---|---|
| 헤드리스 CLI — 대화형 로그인만 | ✅ | ❌ 불가 |
| 헤드리스 CLI — **장기 토큰 발급** | ✅ | ✅ |
| API 키 | ✅ | ✅ |
| 사내 게이트웨이 | 사내망이면 ✅ | ✅ |

헤드리스 방식은 기본적으로 **그 기계의 로그인 세션**에 의존한다. 클라우드에는 그게 없다.

**다만 장기 인증 토큰을 발급하면 클라우드에서도 쓸 수 있다.**

```bash
claude setup-token      # 구독 계정으로 장기 토큰 발급
```

토큰을 시크릿에 넣고 실행 환경에 주입하면 된다. API 키를 따로 만들거나 사용료를 낼 필요가 없다.
⚠️ 실행 환경에 **CLI 자체는 설치돼 있어야 한다.**

그래도 `LlmClient` 는 인터페이스로 분리해둔다(2.3). 사내 게이트웨이나 API 로 옮길 일이 생기면
구현체만 추가하면 된다.

### 12.4 macOS — launchd

cron 보다 launchd 를 쓴다. **놓친 실행을 따라잡아 주기 때문이다.**

| 상태 | 동작 |
|---|---|
| 켜져 있음 | 예정 시각에 실행 |
| 잠자기 | 깨어날 때 한 번 따라잡아 실행 |
| 꺼져 있음 | 다음 부팅 후 실행 |

⚠️ 3일 놓쳤다고 3번 돌지 않는다. **한 번만** 돈다. 볼륨이 작으면 문제가 없지만, 실행마다 처리량 상한이 있는 설계라면 밀린 분량을 못 따라잡는다.

⚠️ **launchd 는 로그인 셸을 거치지 않아 PATH 가 거의 비어 있다.** 터미널에서 되던 명령이 launchd 에서 "command not found" 로 죽는 가장 흔한 원인이다. 실행 스크립트에서 PATH 를 명시한다.

```bash
export PATH="$HOME/.local/bin:/opt/homebrew/opt/node@22/bin:/opt/homebrew/bin:/usr/bin:/bin"
```

**검증은 반드시 빈 환경에서 한다.** 터미널에서 잘 돌아도 launchd 에서 죽는다.

```bash
env -i HOME="$HOME" /bin/bash scripts/run.sh
```

**등록·해제**

```bash
cp scripts/<label>.plist ~/Library/LaunchAgents/
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/<label>.plist
launchctl kickstart  gui/$(id -u)/<label>     # 즉시 한 번 실행
launchctl bootout    gui/$(id -u)/<label>     # 해제
```

`RunAtLoad` 는 `false` 로 둔다. 로그인할 때마다 도는 건 대개 원치 않는 동작이다.

### 12.5 백필은 알리지 않는다 🔒

처음 가동하면 과거 리뷰가 한꺼번에 들어온다. **그대로 두면 수십 건이 검수 창구에 쏟아진다.**

과거 리뷰를 가져오는 목적은 답변이 아니라 **택소노미·프롬프트·가드레일을 다듬을 재료**를 얻는 것이다. 2년 전 리뷰에 지금 답을 달 일은 없다.

**날짜 기준으로 전송만 건너뛴다.** 수집·분류·초안은 정상적으로 한다.

```yaml
notify:
  skip_before: "2026-08-25"   # 시스템 가동 시작일
```

| 리뷰 작성일 | 저장 | 분류·초안 | 알림 |
|---|---|---|---|
| 기준일 이전 | ✅ | ✅ | ❌ |
| 이후 | ✅ | ✅ | ✅ |

⚠️ **일회성 플래그로 만들지 않는다.** 설정으로 두면 저장소를 다시 만들거나 다른 환경으로 옮겨도 동작이 같다. `--skip-notify` 같은 실행 인자는 한 번 빠뜨리면 과거 리뷰가 전부 나간다.

### 12.6 로그

자동 실행은 **보이지 않는 곳에서 실패한다.** 로그가 없으면 몇 주 뒤에야 알아챈다.

- 단계별 시작·종료와 결과를 남긴다
- 실패하면 원인을 같이 남긴다
- 로그가 무한정 커지지 않게 잘라낸다 (`tail -n 2000`)
- launchd 의 `StandardOutPath` / `StandardErrorPath` 도 지정해 둔다 — 스크립트가 시작조차 못 했을 때 여기 남는다

### 12.7 클라우드로 옮길 때

| | 제약 |
|---|---|
| **GitHub Actions** | 스케줄이 지연될 수 있다. 리포지토리 활동이 없으면 워크플로가 비활성화되기도 한다 |
| **Vercel Cron** | Hobby 는 1일 1회 · 함수 30초 제한. 백필처럼 오래 걸리는 작업은 못 돌린다 |
| **Cloud Run Job + Scheduler** | 컨테이너를 만들어야 하지만 실행 시간 제약이 느슨하다 |

⚠️ **로컬 파일 저장소(SQLite)는 클라우드에서 못 쓴다.** 실행이 끝나면 파일시스템이 사라진다.

선택지는 둘이다.

| | 언제 |
|---|---|
| **오브젝트 스토리지에 DB 파일 보관** (실행 앞뒤로 내려받고 올림) | 하루 한 번 도는 단일 작업. 새 계정을 안 만들어도 된다 |
| **관리형 DB** (Turso · Postgres 등) | 실행이 겹칠 수 있거나 DB 가 50MB 를 넘을 때 |

전자는 정식 DB 가 아니라 파일 동기화다. **실행이 겹치면 나중 것이 앞의 것을 덮어쓴다.**
리뷰가 드문 서비스에서는 충분하지만, 주기를 줄이는 순간 깨진다.

⚠️ **비용을 기준으로 고르지 않는다.** 리뷰 1건당 약 1KB 라 어떤 규모에서도 저장비는 무시할 수준이다.
무너지는 건 언제나 **동기화 방식**이 먼저다.

⚠️ **초기 백필은 클라우드에서 하지 않는다.** 과거 리뷰 수십~수백 건을 한 번에 처리하면 실행 시간 제한에 걸린다. **로컬에서 백필을 끝내고 DB 만 올리는 편이 빠르다.**

---

## 13. 개인 의존 제거 — 담당자가 떠나도 돌아가게

만든 사람이 조직을 떠나면 멈추는 시스템이 흔하다. **서버를 옮기는 문제가 아니라 소유권 문제다.**

### 13.1 무엇이 사람에게 묶이는가

| 항목 | 개인에 묶임 | 조직 소유로 |
|---|---|---|
| **App Store Connect 개별 키** | ⚠️ **사용자 계정에 묶인다. 그 사용자가 제거되면 키도 무효** | **팀 키**로 교체 |
| 코딩 에이전트 구독 | 개인 계정 | 회사 명의 API 키 |
| 실행 장비 | 개인 노트북 | 회사 인프라 |
| Google Play 서비스 계정 | 조직 GCP 프로젝트 소유 ✅ | 그대로 |
| 슬랙 앱 | 워크스페이스 소유 ✅ | 소유자 이관만 확인 |
| 코드 저장소 | 개인 계정이면 위험 | 조직 계정으로 |

⚠️ **ASC 개별 키가 가장 위험하다.** 편해서 개별 키로 시작하기 쉬운데, 퇴사·이동으로 계정이 정리되는 순간 파이프라인이 조용히 죽는다. 그리고 **아무도 그 사실을 모른다** — 리뷰 알림이 안 오는 걸로만 나타난다.

운영으로 넘길 때는 팀 키로 바꾼다. JWT payload 가 `sub` 에서 `iss` 로 바뀐다(2.2).

### 13.2 이관 체크리스트

```
[ ] ASC 개별 키 → 팀 키 교체
[ ] LLM 을 회사 명의 API 키 또는 사내 게이트웨이로 교체
[ ] 저장소를 로컬 파일 → 호스팅 DB 로 이전
[ ] 실행을 개인 장비 → 조직 인프라로 이전
[ ] 코드를 조직 저장소로 이전
[ ] 시크릿을 조직 시크릿 저장소로 이전 (개인 로컬 파일 정리)
[ ] 슬랙 앱·채널 소유자 확인
[ ] 인수인계 문서 작성 (13.3)
```

### 13.3 인수인계 문서에 반드시 들어갈 것

가장 자주 빠지는 산출물이다. 코드는 남는데 **운영 지식이 사라진다.**

- 자격증명이 각각 무엇이고 **어디서 발급했는지** (재발급 경로)
- **만료가 있는 것과 그 주기** — 키·토큰·인증서
- 실패했을 때 **어디를 보는지** (로그 위치, 알림 채널)
- 택소노미·답변 정책을 **누가 바꿀 권한이 있는지**
- 파이프라인이 **멈췄는지 어떻게 아는지** ← 아래 참조

### 13.4 조용한 실패를 감지한다 🔒

⚠️ **리뷰가 드문 서비스일수록 위험하다.** 월 1~2건이면 "알림이 안 온다"와 "파이프라인이 죽었다"를 구분할 수 없다. 몇 달 뒤에야 알아챈다.

**살아있음 신호(heartbeat)를 만든다.**

- 주 1회, 새 리뷰가 없어도 "이번 주 새 리뷰 0건, 파이프라인 정상" 을 알린다
- 또는 마지막 성공 실행 시각을 기록하고, 일정 기간 넘으면 경고를 보낸다

조용한 게 정상인 시스템은 **조용함이 정상인지 고장인지 알려줘야 한다.**

---

## 14. 서비스 적용 워크시트

새 서비스에 적용할 때 **이 표만 새로 채운다. 코드는 건드리지 않는다.**

```yaml
# ── 서비스 ──────────────────────────────────
service:
  name: ""                    # 사용자에게 보이는 서비스명
  type: ""                    # commerce | education | game | subscription | finance | ...
  context: ""                 # 모델에게 줄 한두 문장 설명
  languages: []               # 실제로 들어오는 리뷰 언어

# ── 소스 ────────────────────────────────────
sources:
  - id: google-play
    package_name: ""
    credentials_path: ""      # 서비스 계정 JSON (gitignore)
    report_bucket: ""         # gs://pubsite_prod_rev_...
  - id: app-store
    app_id: ""
    key_id: ""
    issuer_id: ""
    p8_path: ""

# ── 수집 ────────────────────────────────────
ingest:
  interval: ""                # 볼륨에 맞춰. 2.4의 표 참조
  backfill_from: ""           # 벌크 임포트 시작 월 (YYYYMM)

# ── 답변 정책 (3.5 참조) ─────────────────────
policy:
  speaker: ""
  formality: ""
  signature: ""
  cannot_promise: []
  allowed_links: []
  do_not_reply: []

# ── 택소노미 (5장 참조) ──────────────────────
taxonomy:
  categories: []              # 실제 리뷰 100건을 읽고 만든다
  fallback: other

# ── 가드레일 (8장 참조) ──────────────────────
guardrails:
  block_categories: []
  block_keywords: []
  forbidden_phrases: []
  min_confidence: 0.75

# ── 알림 ────────────────────────────────────
notify:
  channel: ""
  skip_before: ""             # 이 날짜 이전 리뷰는 알리지 않는다 (백필용, 12.5)

# ── 검수 ────────────────────────────────────
review_ui:
  form: ""                    # slack | cli | web | spreadsheet
  reviewer: ""                # 누가 검수하는가

# ── 자동 실행 (12장) ────────────────────────
schedule:
  runner: ""                  # launchd | cron | github-actions | cloud-run | vercel
  time: ""                    # 예: "10:00 KST"
  llm_auth: ""                # headless-cli | api-key | internal-gateway
                              # ⚠️ headless-cli 는 클라우드에서 못 쓴다

# ── 소유권 (13장) ───────────────────────────
ownership:
  asc_key_kind: ""            # individual | team
                              # ⚠️ individual 은 발급자 계정이 사라지면 죽는다
  runs_on: ""                 # personal-laptop | company-infra
  secrets_store: ""           # local | 조직 시크릿 저장소
  handover_doc: ""            # 인수인계 문서 위치
```

### 볼륨에 따른 선택 🔧

| 월 유입 | 수집 주기 | 검수 형태 | 주 지표 |
|---|---|---|---|
| ~10건 | 1일 1회 | 슬랙 · CLI · 스프레드시트 | 초안 채택률 |
| 10~100건 | 6시간 | 슬랙 · 간단한 웹 화면 | 채택률 + 정확도 |
| 100건~ | 1시간 · 웹훅 | 큐 기반 검수 UI | 정확도 · 처리량 · SLA |

⚠️ **택소노미는 재사용하지 않는다.** 서비스가 다르면 고객이 말하는 주제도 다르다. 구조는 물려받되 내용은 매번 새로 뽑는 편이 결국 빠르다.

---

## 15. AI 에이전트에게 요청하는 법

이 파일과 채운 워크시트를 함께 주고 아래처럼 요청한다.

### 15.1 전체 구현 요청

```
첨부한 GUIDEBOOK.md 를 읽고, 함께 첨부한 워크시트 설정에 맞춰
앱 리뷰 대응 자동화 파이프라인을 구현해줘.

- 언어/런타임: {{선택}}
- 저장소: {{SQLite | Postgres | ...}}
- 검수 UI: 워크시트의 review_ui.form 을 따를 것

구현 순서는 가이드북 순서를 따르되, 각 단계가 끝날 때마다
동작을 확인할 수 있는 방법을 함께 알려줘.
자격증명은 이미 발급돼 있고 경로는 워크시트에 있다.
```

### 15.2 단계별 요청 (권장)

한 번에 다 시키면 검증이 어렵다. 나눠서 시킨다.

```
1단계 — 가이드북 3·4장대로 데이터 모델과 소스 어댑터를 만들어줘.
        Google Play 어댑터만 먼저. 실제로 리뷰가 수집되는지 확인할 수 있게.

2단계 — 4.3의 CSV 벌크 임포트를 붙여줘. UTF-16 처리 주의.

3단계 — 5장 절차대로 수집된 리뷰를 읽고 택소노미 초안을 제안해줘.
        내가 확정하면 설정 파일로 만들어줘.

4단계 — 6장 프롬프트로 분류 에이전트를 만들고, 수집된 리뷰 전체를 분류해줘.

5단계 — 7장으로 초안 에이전트, 8장으로 가드레일.

6단계 — 9장 명세대로 검수 화면.

7단계 — 10장 발행. 단, 실제 발행은 내 확인 후에만.
```

### 15.3 에이전트에게 반드시 전달할 제약

```
- 자동 발행 기능은 만들지 마. 사람 승인 없이 스토어에 글이 나가면 안 돼.
- 검수 화면에는 초안과 최종본의 차이를 저장하는 기능이 반드시 있어야 해.
- 서비스 고유값(택소노미, 톤, 금칙어, 임계값)을 코드에 하드코딩하지 마.
  전부 설정 파일에서 읽어야 해.
- 원본 응답은 raw 필드에 통째로 저장해.
- Google Play CSV 는 UTF-16 LE 다.
```

---

## 부록 A. 자주 걸리는 문제

| 증상 | 원인 | 조치 |
|---|---|---|
| 토큰은 나오는데 API 403 | Google Play Developer API 미사용 설정 | GCP → 라이브러리에서 사용 설정 |
| `reviews.list` 가 0건 | 최근 7일 내 텍스트 리뷰 없음 | 정상. CSV 벌크 임포트 사용 |
| 버킷 **404** | 버킷 이름 오류 | 콘솔에서 URI 복사. 개발자 ID와 다른 숫자다 |
| 버킷 **403** | 권한 전파 대기 | 최대 24시간. 다음 날 재확인 |
| CSV 가 깨져 보임 | UTF-16 LE + BOM (`fffe`) | BOM 을 보고 분기해 변환. BE(`feff`) 는 `swap16()` 후 읽는다 |
| Play 리뷰의 `starRating` 이 undefined | 본문이 `comments[].userComment` 안에 있다 | 4.2 응답 구조 참조. 인덱스 말고 키 존재로 찾는다 |
| Play 날짜가 1970년으로 나옴 | `lastModified.seconds` 는 **초 단위 문자열** | `Number(seconds) * 1000` |
| Play 2페이지부터 안 넘어감 | 파라미터 이름이 `pageToken` 이 아니라 `token` | 응답의 `tokenPagination.nextPageToken` 을 `token` 에 넣는다 |
| **LLM 응답 `JSON.parse` 실패** | 봉투 + ` ```json ` 펜스 이중 포장 | 2.3 의 두 단계 파싱 |
| 답변에 백틱이 섞여 나감 | 모델이 본문을 코드 블록에 넣음 | 자유 텍스트에도 펜스 제거 적용 |
| 없는 카테고리가 저장됨 | 모델이 스키마를 벗어난 값을 냄 | 파싱 후 택소노미 대조, 없으면 fallback |
| 기기명이 `gta4xlwifi` 처럼 나옴 | API 는 마케팅명이 아니라 코드명을 준다 | 필요한 카테고리에서만 노출 (9.7) |
| 슬랙 `missing_scope` | Bot Token Scopes 에 `chat:write` 없음 | 추가 후 Reinstall to Workspace |
| 슬랙 `not_in_channel` | 봇이 채널에 없다 | 채널에서 `/invite @봇이름`. 이름은 `auth.test` 로 확인 |
| 슬랙 스레드를 못 담 | Incoming Webhook 은 `ts` 를 안 준다 | 봇 토큰 + `chat.postMessage` 로 `ts` 를 받아 `thread_ts` 에 넘긴다 |
| 슬랙 메시지 삭제가 `channel_not_found` | `chat.delete` 는 채널 이름이 아니라 **ID** 를 요구한다 | `postMessage` 응답의 `channel` 을 저장해뒀다가 쓴다 |
| **launchd 에서만 command not found** | 로그인 셸을 안 거쳐 PATH 가 비어 있다 | 스크립트에서 PATH 명시. `env -i` 로 검증 (12.4) |
| 스케줄러에 올렸더니 LLM 호출 실패 | 헤드리스 CLI 가 로그인 세션을 못 찾는다 | 장기 토큰 발급 후 환경변수로 주입. CLI 설치도 확인 (12.3) |
| 클라우드에서 데이터가 사라짐 | 실행 후 파일시스템이 초기화된다 | 호스팅 DB 로 이전 (12.7) |
| 몇 달째 알림이 없다 | 리뷰가 없는 건지 죽은 건지 구분 불가 | heartbeat 를 만든다 (13.4) |
| 콘솔 평점 수 ≠ API 건수 | 별점만 남긴 리뷰는 API에 없음 | 정상 |
| 답변이 잘림 | 350자 초과 | 가드레일 ②에서 사전 차단 |
| 답변이 안 보임 | App Store 반영 지연 | 최대 24시간 대기 |
| ASC 팀 키에 '고객 지원' 역할이 없음 | 팀 키는 그 역할을 배정 못 한다 | 개인 키를 쓰거나 팀 키에 Admin 부여 (2.2) |
| ASC **401** | ES256 서명이 DER 형식 | `dsaEncoding: 'ieee-p1363'` 로 서명 |
| ASC 401 (개인 키) | payload 에 `iss` 를 넣었다 | 개인 키는 `iss` 대신 `sub: "user"` |
| ASC 401 (팀 키) | payload 에 `sub` 를 넣었다 | 팀 키는 `iss: "<Issuer ID>"` |
| ASC 401 (서명은 맞는데) | JWT `exp` 가 20분 초과 | 수명을 20분 이내로 |
| ASC 응답에 appVersion 없음 | 원래 없는 필드다 | 정규화 시 undefined 처리 |
| **RSS 는 0건인데 콘솔엔 리뷰가 있음** | **RSS 피드가 신뢰할 수 없다** | **RSS 결과로 판단하지 말 것. 콘솔·정식 API 로 확인** |
| 한쪽 스토어만 답변이 달려 있음 | 담당자가 한 플랫폼만 관리 | 양쪽 다 확인. 기존 답변은 few-shot 재료다 (7.3) |
| ASC 기존 답변 본문이 안 옴 | relationships 만으로는 부족 | `include=response` 추가 |

## 부록 B. 참고

- [Reply to Reviews — Google Play Developer API](https://developers.google.com/android-publisher/reply-to-reviews)
- [Getting Started — Google Play Developer API](https://developers.google.com/android-publisher/getting_started)
- [Download and export monthly reports — Play Console Help](https://support.google.com/googleplay/android-developer/answer/6135870)
- [Create or update a response to a customer review — Apple](https://developer.apple.com/documentation/appstoreconnectapi/post-v1-customerreviewresponses)
- [현대차그룹이 AI를 경쟁력으로 만드는 방법](https://www.hyundaimotorgroup.com/ko/story/hyundai-motor-group-ai-competitiveness)

---

*이 가이드북은 실제 구축 과정에서 확인된 내용을 반영한다. 새로 걸린 문제가 있으면 부록 A에 추가한다.*
