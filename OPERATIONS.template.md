# 운영 문서 (템플릿)

> `OPERATIONS.md` 로 복사해서 `<...>` 부분을 채운다.
>
> **가장 자주 빠지는 산출물이다.** 코드는 저장소에 남는데 운영 지식은 사라진다.
> 후임이 정해진 상태에서 인수인계하면 말로 채울 수 있지만, 미정인 채로 떠나면
> 그 사람은 아무 맥락 없이 이 시스템을 마주한다. 이 문서가 유일한 연결고리다.

---

**한 줄 요약** — 매일 `<시각>` `<실행 환경>` 이 앱 리뷰를 가져와 분류하고
답변 초안을 만들어 `<채널>` 로 보낸다. 사람이 콘솔에서 답변을 등록한다.

---

## 1. 자격증명

| 이름 | 무엇 | 발급처 | 소유 | 만료 |
|---|---|---|---|---|
| `GCP_SA_KEY` | 서비스 계정 JSON (base64) | GCP `<프로젝트>` | 조직 | 없음 |
| `ASC_P8` | App Store Connect 키 (base64) | App Store Connect | `<팀/개인>` | 없음 |
| `SLACK_BOT_TOKEN` | 슬랙 봇 토큰 | Slack 앱 `<앱이름>` | 워크스페이스 | 없음 |
| `<LLM 인증>` | | | `<소유>` | `<만료>` |

저장 위치: `<GitHub Secrets / Secret Manager / ...>`

로컬 실행용 파일은 `secrets/` 에 있고 `.gitignore` 대상이다.
**파일 이름이 워크시트의 경로와 정확히 일치해야 한다.** 실행 환경이 시크릿을 그 이름으로 복원한다.

### ⚠️ 개인에 묶인 것을 여기 적는다

<!--
  조직 계정으로 발급했어도 **토큰 자체는 발급한 사람 것**인 경우가 있다.
  그 사람이 떠나면 조용히 멈춘다. 어떤 증상으로 나타나는지까지 적어둔다.

  예) LLM 토큰이 죽으면 수집·알림은 계속 돌고 초안만 안 붙는다.
      "리뷰는 오는데 초안이 없는" 형태라 알아채기 어렵다.
-->

---

## 2. 자격증명 교체

### 2-1. `<담당자가 바뀔 때 교체할 것>`

```bash
<발급 명령>
<등록 명령>
```

⚠️ **시크릿 이름을 반드시 명령에 포함한다.** 이름을 빠뜨리면 도구가 붙여넣은 값을
*이름* 으로 받아 **에러 메시지에 시크릿이 그대로 노출된다.**

```bash
gh secret set <이름> --repo <저장소>   # ✅
gh secret set --repo <저장소>          # ❌
```

노출됐다면 즉시 재발급하고 셸 히스토리를 지운다.

```bash
history | grep -n "<접두사>"
history -d <줄번호>
```

**확인** — 교체 후 실제로 동작하는지 본다.

```bash
npm run verify
```

### 2-2. App Store Connect 키

1. App Store Connect → 사용자 및 액세스 → 통합 → **팀 키** → `+`
2. `.p8` 다운로드 ⚠️ **한 번만 받을 수 있다**
3. Key ID 와 Issuer ID 확보

워크시트의 `key_id` / `issuer_id` / `p8_path` 를 같이 고친다.

⚠️ **팀 키와 개별 키는 JWT payload 가 다르다.** 팀 키는 `iss`, 개별 키는 `sub`.
섞으면 401 만 본다. `key_kind` 를 반드시 맞춘다.

⚠️ **개별 키는 발급자 계정에 묶인다.** 운영 시스템에는 팀 키를 쓴다.

확인 후 기존 키를 무효화한다.

### 2-3. GCP 서비스 계정 키

새 JSON 키를 발급해 등록하고 옛 키를 삭제한다.

⚠️ 이 계정이 **어디어디서 권한을 받는지** 적어둔다. 키만 바꾸면 유지되지만
계정을 새로 만들면 전부 다시 줘야 한다.

- `<Play Console 앱 권한>`
- `<Play Console 계정 권한>`
- `<GCS 버킷 권한>`

⚠️ Play Console 권한은 반영에 **최대 24시간**. GCP IAM 은 즉시.

### 2-4. 슬랙 봇 토큰

앱 설정 → OAuth & Permissions → Reinstall to Workspace

⚠️ 봇이 채널에 없으면 `not_in_channel`. 채널에서 `/invite @<봇이름>`.

---

## 3. 고장났을 때

### 실패 알림이 왔다

| 로그에 보이는 것 | 원인 | 조치 |
|---|---|---|
| `401` (ASC) | 키 종류 불일치 또는 만료 | 2-2 |
| `403` (스토리지) | 서비스 계정 권한 | 2-3 |
| `missing_scope` / `not_in_channel` | 슬랙 설정 | 2-4 |
| 분류·초안 단계 실패 | LLM 인증 만료 | 2-1 |

### 살아있음 신호가 🔴 로 왔다

`<임계 시간>` 넘게 정상 완료 기록이 없다는 뜻이다.

1. 실행 이력을 본다
2. **실행 자체가 없으면 워크플로가 비활성화됐을 수 있다.**
   공개 저장소는 60일간 커밋이 없으면 GitHub 이 스케줄 워크플로를 끈다.
   (비공개는 문서에 명시가 없다.) 수동 실행하면 다시 켜진다
3. 실행이 실패했으면 위 표를 따른다

### 알림이 안 온다

**리뷰가 정말 없는 것일 수 있다.** 월 유입이 적으면 조용한 게 정상이다.
살아있음 신호가 🟢 면 파이프라인은 살아 있다.

### 같은 리뷰가 두 번 왔다

저장소 동기화가 깨졌다. 실행이 겹쳤을 가능성이 높다.

```bash
npm run db:verify
```

---

## 4. 무엇을 바꿀 때 어디를 고치나

| 바꾸고 싶은 것 | 고칠 곳 |
|---|---|
| 답변 톤·서명·금지 표현 | `worksheet.yaml` → `policy` |
| 분류 카테고리·태그 | `worksheet.yaml` → `taxonomy`, `tag_labels` |
| 자동 초안을 막을 카테고리 | `worksheet.yaml` → `guardrails.block_categories` |
| 금칙어·차단 키워드 | `worksheet.yaml` → `guardrails` |
| 알림 채널 | `worksheet.yaml` → `notify.channel` |
| 실행 시각 | `.github/workflows/*.yml` → `cron` |
| 답변 길이 목표 | `worksheet.yaml` → `policy.target_reply_length` |

고친 뒤 검사한다.

```bash
npm run worksheet:check
```

⚠️ **택소노미를 바꿔도 기존 분류는 그대로 남는다.** 과거 리뷰까지 새 기준으로 보려면
`review_case` 를 지우고 다시 처리한다. 원본(`review`)은 남아 있으므로 가능하다.

---

## 5. 이 서비스의 특이사항

<!--
  일반 문서에 없는, 이 서비스에서만 걸리는 것들을 적는다.

  예)
  - 미해결 항목과 그 영향 범위
  - 볼륨이 작아 지표 해석에 주의할 점
  - 특정 카테고리가 유독 많은 이유
  - 과거에 겪은 사고와 재발 방지책
-->

---

## 6. 관련 문서

| | |
|---|---|
| `README.md` | 설치·실행·구조 |
| `GUIDEBOOK.md` | 왜 이렇게 만들었는지. 다른 서비스에 적용할 때 |
| `worksheet.yaml` | 이 서비스의 고유값 전부 |
